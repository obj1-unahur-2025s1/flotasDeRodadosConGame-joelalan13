import wollok.game.*
class Corsa {
    /*uso property por que puede preguntarle al corsa su color, y asignarle un color
    //COMO crea un setter y un getter el property, donde no debo cambiarle un valor , no es necesario usar property
    //aca como es algo que puedo "yo " cambiarle esta bien
    // las clases funcionaran como un "molde", empieza mayuscula
    //new Corsa(method="valor == uncolor") estructura para crear un objecto de esta clase desde terminal
    //para acceder a ese objecto a traves de una referencia "como sucede con una lista",
    debemos que crear una lista ya que esta tienen un indice const listaDeAutos =[new Corsa(color="rojo")]
    listaDeAutos.add(new Corsa(color="rojo"))
    (conocer un objeto a traves de una referencia)
    el corsa no tienen metodo de tanque adicional, por que a nadie le intersa o preguntaria
    usamos string por que no son usados para comportamientos
    */
    //position tine eje X y eje Y
    var property color
    const pasoPor = []
    var position = new Position(x=4,y=7) //game.st(0,0)
    method colorAuto(unColor){}
    method capacidad(){4}
    method velocidadMaxima(){150}
    method peso(){1300}
    method position(nuevaPosicion)
}

class Kwid {
    var property tieneTanqueAdicional 

    method capacidad() =
        if(tieneTanqueAdicional){
            3
        }
        else{
            4
        }
    method velocidadMaxima() = 
        if(tieneTanqueAdicional){
            110
        }else{
            120
        }
    method peso() = 1200 + if(tieneTanqueAdicional){150}else{0}
    method color() = "azul"
}

object trafic{
    var property interior = comodo
    var property motor = pulenta
    method capacidad() = interior.capacidad()
    method peso() = 4000 + interior.peso() + motor.peso()
    method velocidadMaxima() = motor.velocidad()
    method color() = "blanco"
}

class Especial {
//les saco el = a la variable ya que no tienen un valor seteado com cuanta capacidad
// con "const property" el atributo es que le asignamos A CADA AUTO no puede modificarse para CADA auto
//para asignarle un valor comun que se usada todos lo objetos y no sea un metodo utilizamos un objeto esterno
//con esto le asignaremos un valor comun con ayuda de un objeto externo
    var property capacidad 
    const velocidadMaxima 
    var property peso
    var property color 
    method velocidadMaxima() = 
    velocidadMaxima.min(topeVelocidadMaxima.tope())
}

object topeVelocidadMaxima{
    var property tope = 200 
}
object comodo{
    method capacidad() = 5
    method peso() = 700
}
object popular{
    method capacidad() = 12
    method peso() = 1000
}

object pulenta{
    method peso() = 800
    method velocidad() = 130
}

object bataton{
    method peso() = 500
    method velocidad() = 80
}
